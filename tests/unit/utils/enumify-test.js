import enumify from 'exocalc/utils/enumify';
import { module, test } from 'qunit';

module('Unit | Utility | enumify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enumify();
  assert.ok(result);
});
